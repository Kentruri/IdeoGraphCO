# IdeoGraphCO — dependencias DIRECTAS.
#
# Antes esto era un `pip freeze` de 135 líneas con todo el árbol transitivo
# (aiohappyeyeballs, asttokens, ptyprocess…) y las herramientas de desarrollo
# mezcladas con las de ejecución. Nadie podía saber qué necesitaba el proyecto
# y qué había arrastrado la instalación.
#
#     python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# Las versiones son las probadas; pip resuelve solo las transitivas.
#
# La INFERENCIA no vive aquí: el servicio HTTP, el predictor y el mapa de
# calor están en IdeoGraphCO-BE. Este repo llega hasta el checkpoint
# entrenado y su evaluación (OE1-OE3).

# --- Scraping y limpieza (OE1) ---
trafilatura==2.0.0
courlan==1.3.2                # extract_links, en parser.py: se importa directo
feedparser==6.0.12
requests==2.33.0

# --- Etiquetado con LLM (silver + filtro) ---
google-genai==1.75.0          # juez Gemini y desempates
python-dotenv==1.2.2
tqdm==4.67.3

# --- Anotación humana (gold) ---
openpyxl==3.1.5               # libros .xlsx: prepare_gold_set / ingest_gold

# --- Corpus compartido ---
huggingface_hub==1.8.0        # scripts/dataset_sync.py (dataset privado)

# --- Entrenamiento y benchmark (OE2/OE3) ---
torch==2.11.0
transformers==5.4.0
pytorch-lightning==2.6.1
torchmetrics==1.9.0
hydra-core==1.3.2
omegaconf==2.3.0
numpy==2.4.3
pandas==3.0.1
PyYAML==6.0.3

# --- Prefilter local (TF-IDF + regresión logística) ---
scikit-learn==1.8.0
joblib==1.5.3

# --- Gráficas del benchmark ---
matplotlib==3.11.1            # scripts/compare_models.py

# --- Desarrollo (no hacen falta para correr el pipeline) ---
pytest==9.0.2
ruff==0.15.8

# Opcional: segundo juez del ensemble silver (src/agents/silver/judges.py).
# Sin él funcionan los jueces Gemini, pero serán de la misma familia y su
# acuerdo medirá consistencia, no corrección.
# anthropic>=0.40
